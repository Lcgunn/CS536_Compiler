logical a.
logical b.
integer c.
integer x.
integer computation {integer f, integer g} [
    integer i.
    integer h.
    i = (f + 4).
    while (g < f) [
            g++.
    ]
    return g.
]
logical compare {logical b, logical k} [
    if k [
        return true.
    ]
    else [
        if b[
        return true.
    ]
    return false.
    ]
]
void rw {integer h} [
    integer f.
    read >> h.
    write << h.
    compare().
    computation(f, 2).
]
tuple Addr {
    integer doorNum.
    integer zipCode.
}.
tuple Student {
    integer id.
    integer age.
    tupple Addr addr.
}.
tupple Student raj.
tupple Student jack.
void test {logical c, integer y, integer b} [
    if (b == -1) [
        x = ((4 + (3 * 5)) - y).
    while c [
            y = ((y * 2) + x).
    ]
    ]
    else [
        x = 0.
    ]
]
