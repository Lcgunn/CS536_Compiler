logical a.
logical b.
integer c.
integer x.
integer computation {integer f, integer g} [
    integer i.
    integer h.
    i = (f + 4).
    while (g < f) [
            g++.
    ]
    return g.
]
logical compare {logical b, logical k} [
    if k [
        return true.
    ]
    else [
        if b[
        return true.
    ]
    return false.
    ]
]
void rw {integer h} [
    integer f.
    read >> h.
    write << h.
    compare().
    computation(f, 2).
]
