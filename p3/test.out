logical a.
logical b.
integer c.
integer x.
void d.
integer hello {integer f, integer g} [
    integer i.
    g++.
    g = (g + 4).
    h = i.
    f = (h + g).
    return f.
]
logical compare {integer b, integer k} [
    if (k < b) [
        return true.
    ]
    else [
        return false.
    ]
]
